'use strict';

process.env.NODE_ENV = 'test';
var chai = require('chai');
var should = chai.should();
require('../app');

describe('Drum Model', function() {
	it('should create beats', function(done) {
		done();
	});

});
